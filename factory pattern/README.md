# factory method
구상 형식 인스턴스를 만드는 과정을 캡슐화한다. 실제 팩토리 메소드를 구현하고,
제품을 생성하는 일은 서브클래스에서만 할 수 있도록 한다.
팩토리 메서드는 직접 객체를 생성하는 것이 아닌(new) 객체 생성에 사용되어야 한다.

# abstract factory method
구상 클래스에 의존하지 않고도 서로 연관되거나, 의존적인 객체로 이루어진 인터페이스를 생성한다.
